import { environment } from "../../environments/environment";

export const BASE_URLS = {
  USERS: environment.baseUrls.users,
  PRODUCTS: environment.baseUrls.products,
  ORDERS: environment.baseUrls.orders
};